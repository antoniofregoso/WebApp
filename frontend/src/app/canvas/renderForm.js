export function renderForm(data={}, lang) {
    return `
    <main id="dashboard-content" class="dash-content" role="main" aria-label="Form">
        <div class="dash-content-inner">
            <div class="dash-content-hero">
                <h2 class="dash-content-title">Form</h2>
                <p class="dash-content-placeholder">This is where the form will be displayed.</p>
            </div>
        </div>
    </main>
    `;
}